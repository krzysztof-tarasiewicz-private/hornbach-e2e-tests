import {When, Then} from "@cucumber/cucumber";
import {page} from "./world";
import {expect} from "@playwright/test";

When("User provides date of birth of the Customer", async () => {

});
Then("User verifies if customer is adult", async () => {

});